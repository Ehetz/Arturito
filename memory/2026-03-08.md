# 2026-03-08

- Email sending credentials exist at `~/.secrets/mail.env` for Gmail SMTP.
- SMTP config reference:
  - host: `smtp.gmail.com`
  - port: `587`
  - user: `andrew.miller.assis@gmail.com`
- Sensitive secret (SMTP app password) is stored in `~/.secrets/mail.env` and should not be copied into workspace memory files.
- 00:49 Europe/Berlin checkpoint run:
  - OpenClaw/package maintenance checked; `openclaw update` skipped (package-manager detection issue), then `npm i -g openclaw@latest` applied.
  - Version mismatch remains (`openclaw --version` reports 2026.3.2 while gateway app reports 2026.3.7); requires service entrypoint alignment.
  - Tool/system health checked via `openclaw status --deep`: Telegram channel OK, gateway reachable.
  - Pipeline had no actionable projects (`pipeline_cli.py next` => none).
  - Docs review touchpoint: docs root indicates using `https://docs.openclaw.ai/llms.txt` as index for discovering upgrade opportunities.
